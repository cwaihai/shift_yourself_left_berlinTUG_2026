[project]
name = "tableau-public-pipeline"
version = "0.1.0"
description = "dlt pipeline: Tableau Public profile + workbook metadata → DuckDB"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dlt[duckdb]>=1.27.0",
]
